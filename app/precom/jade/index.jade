doctype
html(lang=es)
	head
		meta(charset="utf-8")
		title Skeletor
		meta(name="description",content="Skeletor")
		meta(name="viewport",content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no")
		link(rel="stylesheet",href="bower_components/fancybox/source/jquery.fancybox.css")
		link(rel="stylesheet",href="css/style.css")
	body
		.link_modal
			a.fancybox.open_modal(data-link-modal="#openModal") Abrir modal



		script(src="bower_components/jquery/dist/jquery.min.js")
		script(src="bower_components/underscore/underscore.js")
		script(src="bower_components/parsleyjs/dist/parsley.min.js")
		script(src="bower_components/fancybox/source/jquery.fancybox.js")
		script(src="js/script.js")

script(type="text/template" id="openModal")
	#modal.modal_city
		.content
			h3.title
				| Seleccione
			.content_group
				.select_group
					.group
						label Departamento :
						select.select_cat(name="depart")
							option(value="Seleccion" selected disabled) Selecciona
					.group
						label Provincia :
						select.select_item(name="prov")
							option(selected disabled) Escoge
					.group
						label Distrito :
						select.select_item(name="dist")
							option(selected disabled) Escoge
				.btn
					.btn_add
						| Agregar
					.btn_update
						| Actualizar
					.btn_loading
						| Espere...


script(type="text/template" id="listSlc")
	| {{ for(i=0;i<data.length;i++){ }}
	| {{ if(id == data[i].id_ubigeo) { }}
	option(value='{{=data[i].id_ubigeo}}' selected) {{=data[i].nombre_ubigeo}}
	| {{
	| } else {
	| }}
	option(value='{{=data[i].id_ubigeo}}' ) {{=data[i].nombre_ubigeo}}
	| {{
	| } }
	| }}

script(type="text/template" id="addDatos")
	| {{ if(!data.length == 0) { }}
	.list_add
		h3.title Lista de datos agregados
		ul.content_list
			li
				div.depart Departemento
				div.prov Provncia
				div.dist Distrito
				div.editar Editar
				div.remover Eliminar
				|{{$.each(data, function(key, value){ }}
				| <li><div class='id' data-id='{{=key}}'></div><div data-id-dpto='{{=value.dpto.id}}'>{{=value.dpto.name}}</div><div data-id-prov='{{=value.prov.id}}'>{{=value.prov.name}}</div><div data-id-dist='{{=value.dist.id}}'>{{=value.dist.name}}</div><div class='edit'>Edit</div><div class='remove'>Eliminar</div></li>
				|{{ }); }}
	|{{ } }}